/**
 * Theme barrel (compat).
 *
 * Prefer explicit trees:
 *   `bibli/shared/theme/legacy` — useTheme, --russ-*
 *   `bibli/shared/theme/rr`     — useRussRedesign*, data-rr
 */
export * from './legacy'
export * from './rr'
